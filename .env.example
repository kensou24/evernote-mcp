# Required: Evernote Developer Token
# Get your token from: https://evernote.com/api/DeveloperToken.action
EVERNOTE_AUTH_TOKEN=your_auth_token_here

# Optional: API backend (default: evernote)
# Options: evernote, china, china:sandbox
EVERNOTE_BACKEND=evernote

# Optional: Network retry count (default: 5)
EVERNOTE_RETRY_COUNT=5

# Optional: Use system SSL CA (default: false)
EVERNOTE_USE_SYSTEM_SSL_CA=false
